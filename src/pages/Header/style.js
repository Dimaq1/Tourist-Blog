import theme from "../../theme/theme"

const style = {
  header: {
    position: 'static',
    height: 100,
    display: 'flex',
    justifyContent: 'center',
    boxShadow: 0,
  },
  headerContainer: {
    display: 'flex',
    justifyContent: 'space-between'
  }
}

export default style